<!-- StarMon: hardware monitoring and control
     Portions copyright © 2023-2024 Piotr Szczepański (GPL-3.0)

     The tray menu.

     Its WinForms predecessor carried two workarounds that this does not need,
     and the reason is worth writing down so neither is reintroduced by habit.

     The first was a ContextMenuStrip subclass that forced WS_EX_TOPMOST onto
     its window at creation, plus a handler raising every drop-down above the
     taskbar once opened. The taskbar is itself an always-on-top window, and a
     menu raised only after becoming visible can lose that race. A WPF Popup
     is already a topmost window of its own.

     The second was clamping the menu's anchor into the working area by hand,
     because a NotifyIcon shows its menu wherever it is told to. The shell's
     version-4 callback protocol reports the anchor the shell wants, and a
     Popup with a screen placement keeps itself on the monitor. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Palette.xaml"/>
        <ResourceDictionary Source="Typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource AcrylicStrong}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontText}"/>
        <Setter Property="FontSize" Value="{StaticResource SizeBody}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource RadiusInset}"
                            Padding="4"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="Background" Value="{StaticResource Divider}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The check mark and the sub-menu arrow are Paths rather than glyphs, so
         they scale with the display and never depend on a font being present -->
    <ControlTemplate x:Key="MenuItemLeaf" TargetType="MenuItem">
        <Border x:Name="Back" Background="Transparent"
                CornerRadius="4" Padding="10,6" Margin="0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Path x:Name="Tick" Grid.Column="0"
                      Data="M 1,6 L 4.5,9.5 L 11,2.5"
                      Stroke="{StaticResource Accent}" StrokeThickness="1.8"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      VerticalAlignment="Center" Opacity="0"/>

                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                  VerticalAlignment="Center"/>

                <TextBlock x:Name="Hint" Grid.Column="2"
                           Text="{TemplateBinding InputGestureText}"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="{StaticResource SizeMicro}"
                           Margin="16,0,0,0" VerticalAlignment="Center"/>

            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Back" Property="Background" Value="{StaticResource Hover}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Tick" Property="Opacity" Value="1"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuItemBranch" TargetType="MenuItem">
        <Grid>
            <Border x:Name="Back" Background="Transparent"
                    CornerRadius="4" Padding="10,6" Margin="0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ContentPresenter Grid.Column="1" ContentSource="Header"
                                      VerticalAlignment="Center"/>

                    <Path Grid.Column="2" Data="M 0,0 L 4,4 L 0,8"
                          Stroke="{StaticResource TextMuted}" StrokeThickness="1.4"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          Margin="18,0,0,0" VerticalAlignment="Center"/>

                </Grid>
            </Border>
            <Popup x:Name="PART_Popup" AllowsTransparency="True"
                   Placement="Right" HorizontalOffset="-2"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                   Focusable="False" PopupAnimation="Fade">
                <Border Background="{StaticResource AcrylicStrong}"
                        BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                        CornerRadius="{StaticResource RadiusInset}" Padding="4"
                        Margin="0,0,8,8">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="14" ShadowDepth="2"
                                          Opacity="0.55" Color="Black"/>
                    </Border.Effect>
                    <StackPanel IsItemsHost="True"/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Back" Property="Background" Value="{StaticResource Hover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Template" Value="{StaticResource MenuItemLeaf}"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource MenuItemBranch}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
